const ProblemPage = require('../pageobjects/Company.page')

describe('Problem functionality', () => {

    before(() => {
        browser.maximizeWindow();
    });


    it('Test', async () => {

    });
});